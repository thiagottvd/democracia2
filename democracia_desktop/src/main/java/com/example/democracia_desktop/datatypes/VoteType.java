package com.example.democracia_desktop.datatypes;

/**
 * An enumeration representing the possible types of votes that can be cast in a poll. A vote can be
 * either "POSITIVE" if the voter approves of the proposal being voted on, or "NEGATIVE" if the
 * voter disapproves.
 */
public enum VoteType {
  POSITIVE,
  NEGATIVE
}
